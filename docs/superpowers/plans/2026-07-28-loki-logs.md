# Logs no Grafana via Loki — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Tornar os logs de warning e erro do Performance Tracker pesquisaveis no Grafana, sem SSH na EC2.

**Architecture:** Loki roda como container novo no compose existente da VPS, sem porta publicada. O Caddy ganha uma rota `/loki/*` protegida por basic auth no dominio que ja existe — sem registro DNS novo, porque o endpoint de push do Loki ja e `/loki/api/v1/push`. Na EC2, o Grafana Alloy roda como servico systemd, le os quatro arquivos de log do PM2 e empurra por HTTPS. Somente conexao de saida da EC2.

**Tech Stack:** Loki 3.7.4 (container), Grafana Alloy 1.18.0 (RPM em Amazon Linux 2023), Caddy 2.11.4, Grafana 11.5.0.

**Spec:** `docs/superpowers/specs/2026-07-28-loki-logs-design.md`

## Global Constraints

- **Versoes fixas, nunca `latest`:** `grafana/loki:3.7.4`. Alloy instalado do repo `rpm.grafana.com`.
- **Retencao: 30 dias** (`retention_period: 720h`). Nao alterar sem decisao do time — os logs contem PII.
- **Nenhum segredo no git.** Senhas em `secrets/` (ja no `.gitignore`); no `.env` vao apenas o usuario e o hash bcrypt.
- **Labels enxutos:** apenas `job`, `stream`, `host`. Nunca rotular por ID, rota ou conteudo de mensagem — explode a cardinalidade do Loki.
- **Excluir arquivos rotacionados** (`*__*.log`) do glob do Alloy, senao cada rotacao do pm2-logrotate e reingerida por inteiro.
- **Regra de ouro do repo:** nada existe so no servidor. Toda config vai versionada, inclusive a do Alloy que roda na EC2.
- **Caddy 2.11:** a diretiva e `basic_auth` (nao `basicauth`, que e alias antigo).
- **Nenhuma porta inbound nova na EC2.** O Alloy so faz conexao de saida.

## File Structure

| Arquivo | Responsabilidade |
|---|---|
| `loki/loki-config.yml` (novo) | Config do Loki: storage filesystem, schema tsdb v13, compactor com retencao |
| `docker-compose.yml` (modificar) | Servico `loki` + volume `pt-obs-loki-data` |
| `caddy/Caddyfile` (modificar) | Rota `/loki/*` com `basic_auth`, antes do proxy do Grafana |
| `grafana/provisioning/datasources/loki.yml` (novo) | Datasource Loki, ao lado do Prometheus |
| `ec2-exporters/alloy-config.alloy` (novo) | Pipeline do Alloy: descoberta, multiline, labels, push |
| `ec2-exporters/install-alloy.sh` (novo) | Instala o Alloy na EC2 e valida |
| `grafana/dashboards/performance-tracker-api.json` (modificar) | Painel de logs de erro |
| `.env.example` (modificar) | `LOKI_PUSH_USER`, `LOKI_PUSH_BCRYPT` |
| `README.md` (modificar) | Secao de logs + geracao da credencial |
| `scripts/backup-volumes.sh` (modificar) | Incluir `pt-obs-loki-data` |

---

### Task 1: Loki rodando e visivel no Grafana

**Files:**
- Create: `loki/loki-config.yml`
- Create: `grafana/provisioning/datasources/loki.yml`
- Modify: `docker-compose.yml`
- Modify: `scripts/backup-volumes.sh`

**Interfaces:**
- Produces: servico `loki` acessivel em `http://loki:3100` na rede interna do compose; datasource Grafana com `uid: loki`.

- [ ] **Step 1: Escrever a verificacao que deve falhar**

```bash
ssh vps-obs 'docker compose -f /opt/observability/docker-compose.yml exec -T prometheus wget -qO- http://loki:3100/ready'
```

- [ ] **Step 2: Rodar e confirmar que falha**

Esperado: erro de resolucao de nome ou conexao recusada — o servico `loki` ainda nao existe.

- [ ] **Step 3: Criar `loki/loki-config.yml`**

```yaml
# Single binary, storage em disco. Nada de multi-tenant: auth_enabled false, e o
# acesso e controlado pelo basic auth do Caddy na borda.
auth_enabled: false

server:
  http_listen_port: 3100
  log_level: warn

common:
  instance_addr: 127.0.0.1
  path_prefix: /loki
  storage:
    filesystem:
      chunks_directory: /loki/chunks
      rules_directory: /loki/rules
  replication_factor: 1
  ring:
    kvstore:
      store: inmemory

schema_config:
  configs:
    - from: 2026-07-28
      store: tsdb
      object_store: filesystem
      schema: v13
      index:
        prefix: index_
        period: 24h

limits_config:
  # 30 dias — decisao do time; os logs contem PII, nao aumentar sem discutir.
  retention_period: 720h
  # Os logs do PM2 podem chegar atrasados numa reinicializacao do Alloy.
  reject_old_samples: true
  reject_old_samples_max_age: 168h
  volume_enabled: true

compactor:
  working_directory: /loki/compactor
  # Sem retention_enabled o retention_period acima e ignorado em silencio.
  retention_enabled: true
  delete_request_store: filesystem
  compaction_interval: 10m
  apply_retention_interval: 1h
```

- [ ] **Step 4: Adicionar o servico ao `docker-compose.yml`**

Inserir apos o servico `prometheus`:

```yaml
  loki:
    image: grafana/loki:3.7.4
    container_name: loki
    restart: unless-stopped
    command:
      - -config.file=/etc/loki/loki-config.yml
    volumes:
      - ./loki/loki-config.yml:/etc/loki/loki-config.yml:ro
      - loki-data:/loki
    # sem ports: so o Caddy (push) e o Grafana (query) falam com ele
```

E na secao `volumes`, junto dos outros:

```yaml
  loki-data:
    name: pt-obs-loki-data
```

- [ ] **Step 5: Incluir o volume novo no backup**

Em `scripts/backup-volumes.sh` e `scripts/restore-volumes.sh`, trocar a lista:

```bash
for vol in pt-obs-prom-data pt-obs-grafana-data pt-obs-caddy-data pt-obs-loki-data; do
```

- [ ] **Step 6: Criar `grafana/provisioning/datasources/loki.yml`**

```yaml
apiVersion: 1
datasources:
  - name: Loki
    uid: loki
    type: loki
    access: proxy
    url: http://loki:3100
    isDefault: false
```

- [ ] **Step 7: Subir e rodar a verificacao do Step 1**

O `--force-recreate` no grafana nao e opcional: `up -d` nao recria container que
ja esta `Running`, e o Grafana so le o provisioning no boot — sem isso o
datasource do Step 8 nunca aparece.

```bash
ssh vps-obs 'cd /opt/observability && git pull && docker compose up -d loki && docker compose up -d --force-recreate grafana'
```

Esperado apos alguns segundos: `ready`. Antes disso o `/ready` responde `503` e
o log mostra `empty ring` — os dois sao transitorios de boot, nao erro de
config. Aguardar ate 60s antes de investigar.

- [ ] **Step 8: Confirmar o datasource no Grafana**

```bash
ssh vps-obs 'cd /opt/observability && PW=$(grep "^GRAFANA_ADMIN_PASSWORD=" .env | cut -d= -f2-) && curl -s -u "admin:$PW" https://teem.wagnerjunior.dev/api/datasources | grep -o "\"type\":\"loki\""'
```

Esperado: `"type":"loki"`.

- [ ] **Step 9: Confirmar que a retencao foi aplicada de fato**

```bash
ssh vps-obs 'docker compose -f /opt/observability/docker-compose.yml exec -T prometheus wget -qO- http://loki:3100/config | grep -A2 retention_period'
```

Esperado: `retention_period: 720h`. Se vier `0s`, o `limits_config` nao carregou.

- [ ] **Step 10: Commit**

```bash
git add loki/loki-config.yml grafana/provisioning/datasources/loki.yml docker-compose.yml scripts/backup-volumes.sh scripts/restore-volumes.sh
git commit -m "feat: add Loki with 30 day retention and provision its datasource"
```

---

### Task 2: Endpoint de push protegido por basic auth

**Files:**
- Modify: `caddy/Caddyfile`
- Modify: `.env.example`
- Modify: `README.md`

**Interfaces:**
- Consumes: servico `loki` da Task 1.
- Produces: `https://teem.wagnerjunior.dev/loki/api/v1/push` exigindo basic auth; variaveis `LOKI_PUSH_USER` e `LOKI_PUSH_BCRYPT` no `.env`.

- [ ] **Step 1: Escrever a verificacao que deve falhar**

```bash
curl -s -o /dev/null -w "%{http_code}\n" --max-time 10 \
  -X POST https://teem.wagnerjunior.dev/loki/api/v1/push
```

- [ ] **Step 2: Rodar e confirmar que falha**

Esperado: `404` — o Caddy manda tudo para o Grafana hoje, que nao conhece essa rota. O alvo e `401`.

- [ ] **Step 3: Gerar a credencial na VPS**

A senha em claro fica so em `secrets/`; para o git vai apenas o hash.

```bash
ssh vps-obs 'cd /opt/observability && umask 077 && \
  openssl rand -base64 30 | tr -d "\n" > secrets/loki_push_password && \
  docker run --rm caddy:2 caddy hash-password --plaintext "$(cat secrets/loki_push_password)"'
```

Guardar o hash impresso — ele vai no `.env` do proximo passo.

- [ ] **Step 4: Preencher o `.env` da VPS**

```bash
ssh vps-obs 'cd /opt/observability && printf "LOKI_PUSH_USER=alloy\nLOKI_PUSH_BCRYPT=<hash-do-step-3>\n" >> .env'
```

- [ ] **Step 5: Reescrever o `caddy/Caddyfile`**

```
{$GRAFANA_DOMAIN} {
    # Push do Alloy. Precisa vir antes do handle generico, senao o Grafana
    # captura a rota e responde 404.
    handle /loki/* {
        basic_auth {
            {$LOKI_PUSH_USER} {$LOKI_PUSH_BCRYPT}
        }
        reverse_proxy loki:3100
    }

    handle {
        reverse_proxy grafana:3000
    }
}
```

- [ ] **Step 6: Passar as variaveis ao container do Caddy**

Em `docker-compose.yml`, no servico `caddy`, adicionar ao `environment`:

```yaml
      - LOKI_PUSH_USER=${LOKI_PUSH_USER}
      - LOKI_PUSH_BCRYPT=${LOKI_PUSH_BCRYPT}
```

- [ ] **Step 7: Validar a config antes de aplicar**

```bash
ssh vps-obs 'cd /opt/observability && docker compose up -d caddy && sleep 5 && docker compose exec -T caddy caddy validate --config /etc/caddy/Caddyfile'
```

Esperado: `Valid configuration`. O hash bcrypt contem `$`; se o Caddy reclamar de variavel nao resolvida, o problema e a substituicao do `.env` — conferir com `docker compose config`.

- [ ] **Step 8: Rodar a verificacao do Step 1**

Esperado agora: `401`.

- [ ] **Step 9: Confirmar que a credencial certa passa**

```bash
ssh vps-obs 'cd /opt/observability && PW=$(cat secrets/loki_push_password) && \
  curl -s -o /dev/null -w "%{http_code}\n" --max-time 10 -u "alloy:$PW" \
  -H "Content-Type: application/json" \
  -d "{\"streams\":[{\"stream\":{\"job\":\"smoke\"},\"values\":[[\"$(date +%s)000000000\",\"teste de push\"]]}]}" \
  https://teem.wagnerjunior.dev/loki/api/v1/push'
```

Esperado: `204`.

- [ ] **Step 10: Confirmar que o Grafana ainda responde**

```bash
curl -s -o /dev/null -w "%{http_code}\n" --max-time 10 https://teem.wagnerjunior.dev/login
```

Esperado: `200`. Se der 404, o `handle` generico ficou na ordem errada.

- [ ] **Step 11: Documentar e commitar**

Adicionar ao `.env.example`:

```
# Credencial do push de logs do Alloy (gerar conforme o README).
LOKI_PUSH_USER=alloy
LOKI_PUSH_BCRYPT=$2a$14$SUBSTITUA_PELO_HASH
```

```bash
git add caddy/Caddyfile docker-compose.yml .env.example README.md
git commit -m "feat: expose the Loki push endpoint behind basic auth"
```

---

### Task 3: Alloy enviando os logs do PM2

**Files:**
- Create: `ec2-exporters/alloy-config.alloy`
- Create: `ec2-exporters/install-alloy.sh`
- Modify: `README.md`

**Interfaces:**
- Consumes: endpoint autenticado da Task 2.
- Produces: streams no Loki com labels `job` (`server`/`cron`), `stream` (`out`/`err`), `host`.

- [ ] **Step 1: Escrever a verificacao que deve falhar**

```bash
ssh vps-obs 'cd /opt/observability && PW=$(grep "^GRAFANA_ADMIN_PASSWORD=" .env | cut -d= -f2-) && \
  curl -s -u "admin:$PW" -G --data-urlencode "query={job=\"cron\"}" \
  "https://teem.wagnerjunior.dev/api/datasources/proxy/uid/loki/loki/api/v1/query_range" | head -c 200'
```

- [ ] **Step 2: Rodar e confirmar que falha**

Esperado: resultado vazio (`"result":[]`) — nada foi enviado ainda.

- [ ] **Step 3: Criar `ec2-exporters/alloy-config.alloy`**

```alloy
// Logs do PM2 -> Loki. Roda na EC2 da aplicacao, so conexao de saida.
//
// O glob exclui *__*.log de proposito: o pm2-logrotate gera
// cron-out__2026-07-27_17-00-42.log a cada rotacao, e sem a exclusao o Alloy
// trata cada rotacao como arquivo novo e reingere o conteudo inteiro.

local.file_match "pm2_server" {
  path_targets = [{
    __path__         = "/home/ec2-user/.pm2/logs/server-out.log",
    job              = "server",
    stream           = "out",
    host             = constants.hostname,
  }, {
    __path__         = "/home/ec2-user/.pm2/logs/server-error.log",
    job              = "server",
    stream           = "err",
    host             = constants.hostname,
  }]
}

local.file_match "pm2_cron" {
  path_targets = [{
    __path__         = "/home/ec2-user/.pm2/logs/cron-out.log",
    job              = "cron",
    stream           = "out",
    host             = constants.hostname,
  }, {
    __path__         = "/home/ec2-user/.pm2/logs/cron-error.log",
    job              = "cron",
    stream           = "err",
    host             = constants.hostname,
  }]
}

loki.source.file "pm2" {
  targets    = concat(local.file_match.pm2_server.targets, local.file_match.pm2_cron.targets)
  forward_to = [loki.process.pm2.receiver]
}

loki.process "pm2" {
  // Junta dumps de objeto multi-linha na entrada anterior.
  //
  // firstline casa "linha que NAO comeca com espaco nem com }":
  //   [hubspot] POST ... responded 429   -> nova entrada
  //   {"level":50,"msg":"..."}           -> nova entrada (pino, sempre 1 linha)
  //     stripeCustomerId: 'cus_...',     -> continuacao (indentada)
  //   }                                  -> continuacao (fecha o objeto)
  //
  // Escrito como classe negada para nao precisar escapar aspas no regex.
  stage.multiline {
    firstline     = "^[^ }]"
    max_wait_time = "3s"
    max_lines     = 200
  }

  forward_to = [loki.write.central.receiver]
}

loki.write "central" {
  endpoint {
    url = "https://teem.wagnerjunior.dev/loki/api/v1/push"
    basic_auth {
      username = sys.env("LOKI_PUSH_USER")
      password = sys.env("LOKI_PUSH_PASSWORD")
    }
  }
}
```

- [ ] **Step 4: Criar `ec2-exporters/install-alloy.sh`**

```bash
#!/usr/bin/env bash
# Instala o Grafana Alloy na EC2 da aplicacao para enviar os logs do PM2 ao Loki.
#
# A senha e lida do STDIN, nunca de argumento nem de variavel na linha de
# comando: argumento de processo e visivel para qualquer usuario local via `ps`.
#
# Uso:
#   LOKI_PUSH_USER=alloy ./install-alloy.sh <<< "$SENHA"
#
# A senha vem do secrets/loki_push_password do host do stack e nunca e commitada.
set -euo pipefail

DIR="$(cd "$(dirname "$0")" && pwd)"
: "${LOKI_PUSH_USER:?defina LOKI_PUSH_USER}"
read -r LOKI_PUSH_PASSWORD || true
[ -n "${LOKI_PUSH_PASSWORD:-}" ] || { echo "FALHOU: envie a senha pelo stdin"; exit 1; }

# Repo oficial da Grafana (Amazon Linux 2023 usa dnf/rpm).
sudo tee /etc/yum.repos.d/grafana.repo >/dev/null <<'EOF'
[grafana]
name=grafana
baseurl=https://rpm.grafana.com
repo_gpgcheck=1
enabled=1
gpgcheck=1
gpgkey=https://rpm.grafana.com/gpg.key
sslverify=1
EOF

sudo dnf install -y alloy

sudo mkdir -p /etc/alloy
sudo cp "${DIR}/alloy-config.alloy" /etc/alloy/config.alloy

# A credencial vai no EnvironmentFile, fora da linha de comando: argumento de
# processo e visivel para qualquer usuario local via `ps`.
sudo tee /etc/alloy/alloy.env >/dev/null <<EOF
LOKI_PUSH_USER=${LOKI_PUSH_USER}
LOKI_PUSH_PASSWORD=${LOKI_PUSH_PASSWORD}
EOF
sudo chmod 600 /etc/alloy/alloy.env

# O usuario alloy precisa atravessar /home/ec2-user para chegar aos logs.
# Medido na EC2: .pm2 e .pm2/logs ja sao 755 e os arquivos ja sao 644 — o unico
# bloqueio e o home, que e 700. Por isso g+x (grupo) e nao o+x (todos): o grupo
# ec2-user nao tem outros membros, entao na pratica so o alloy ganha travessia.
sudo usermod -a -G ec2-user alloy
sudo chmod g+x /home/ec2-user

sudo mkdir -p /etc/systemd/system/alloy.service.d
sudo tee /etc/systemd/system/alloy.service.d/override.conf >/dev/null <<'EOF'
[Service]
EnvironmentFile=/etc/alloy/alloy.env
EOF

sudo systemctl daemon-reload
sudo systemctl enable --now alloy
sudo systemctl restart alloy

echo "--- validacao ---"
for _ in $(seq 15); do
  systemctl is-active --quiet alloy && break
  sleep 1
done
systemctl is-active --quiet alloy || { echo "FALHOU: alloy nao esta ativo"; sudo journalctl -u alloy -n 30 --no-pager; exit 1; }
echo "OK: alloy ativo"

echo "--- nenhuma porta inbound nova ---"
sudo ss -tlnp | grep -E ":(12345|4040)" && { echo "ATENCAO: alloy abriu porta de escuta"; exit 1; } || echo "OK: sem porta nova"
```

- [ ] **Step 5: Copiar os arquivos e instalar**

A senha viaja por stdin, encadeando os dois SSH. Ela nunca aparece em `ps` nem
no historico de shell de nenhuma das maquinas.

```bash
cd /Users/wagnerjunior/Workdir/Teem/pt-observability
ssh ec2-pt 'mkdir -p /tmp/alloy-install'
scp ec2-exporters/alloy-config.alloy ec2-exporters/install-alloy.sh ec2-pt:/tmp/alloy-install/
ssh vps-obs 'cat /opt/observability/secrets/loki_push_password' \
  | ssh ec2-pt 'cd /tmp/alloy-install && chmod +x install-alloy.sh && LOKI_PUSH_USER=alloy ./install-alloy.sh'
```

- [ ] **Step 6: Rodar a verificacao do Step 1**

Esperado agora: JSON com entradas de log e labels `job="cron"`.

Se vier vazio, checar `sudo journalctl -u alloy -n 50 --no-pager` na EC2. Causa mais comum: permissao de leitura em `/home/ec2-user/.pm2/logs`.

- [ ] **Step 7: Provocar um erro e confirmar que aparece em menos de 30s**

```bash
ssh ec2-pt 'echo "[smoke-test] erro proposital $(date +%s)" >> /home/ec2-user/.pm2/logs/cron-error.log'
sleep 30
ssh vps-obs 'cd /opt/observability && PW=$(grep "^GRAFANA_ADMIN_PASSWORD=" .env | cut -d= -f2-) && \
  curl -s -u "admin:$PW" -G --data-urlencode "query={job=\"cron\",stream=\"err\"} |= \"smoke-test\"" \
  "https://teem.wagnerjunior.dev/api/datasources/proxy/uid/loki/loki/api/v1/query_range" | grep -c smoke-test'
```

Esperado: pelo menos `1`.

- [ ] **Step 8: Confirmar que nao ha reingestao de arquivo rotacionado**

```bash
ssh vps-obs 'cd /opt/observability && PW=$(grep "^GRAFANA_ADMIN_PASSWORD=" .env | cut -d= -f2-) && \
  curl -s -u "admin:$PW" -G --data-urlencode "query=count_over_time({job=\"cron\"}[1h])" \
  "https://teem.wagnerjunior.dev/api/datasources/proxy/uid/loki/loki/api/v1/query" '
```

Anotar o valor, aguardar uma rotacao do pm2-logrotate (ou forcar com `pm2 reloadLogs`) e repetir. O numero deve crescer de forma continua, nao dar um salto de milhares.

- [ ] **Step 9: Confirmar que os labels ficaram enxutos**

```bash
ssh vps-obs 'cd /opt/observability && PW=$(grep "^GRAFANA_ADMIN_PASSWORD=" .env | cut -d= -f2-) && \
  curl -s -u "admin:$PW" "https://teem.wagnerjunior.dev/api/datasources/proxy/uid/loki/loki/api/v1/labels"'
```

Esperado: apenas `job`, `stream`, `host` (alem dos internos do Loki). Qualquer label a mais e cardinalidade indevida.

- [ ] **Step 10: Commit**

```bash
git add ec2-exporters/alloy-config.alloy ec2-exporters/install-alloy.sh README.md
git commit -m "feat: ship PM2 logs from the EC2 to Loki with Grafana Alloy"
```

---

### Task 4: Painel de logs de erro no dashboard

**Files:**
- Create: `grafana/dashboards/build-performance-tracker-api.py`
- Modify: `grafana/dashboards/performance-tracker-api.json`

**Interfaces:**
- Consumes: datasource `loki` da Task 1, streams da Task 3.

> **Antes de comecar:** o JSON do dashboard foi gerado por um script Python que
> nunca foi versionado — ele existe apenas no scratchpad da sessao que criou a
> Fase 5. Editar o JSON a mao agora cria uma armadilha: a proxima regeracao
> apaga o painel de logs em silencio. O Step 1 corrige isso trazendo o gerador
> para o repo, e so depois o painel e adicionado.

- [ ] **Step 0: Versionar o gerador do dashboard**

Copiar o script que produziu `performance-tracker-api.json` para
`grafana/dashboards/build-performance-tracker-api.py` e confirmar que ele
reproduz o arquivo atual byte a byte:

```bash
cd grafana/dashboards
cp performance-tracker-api.json /tmp/antes.json
python3 build-performance-tracker-api.py   # roda a partir da raiz do repo
diff /tmp/antes.json performance-tracker-api.json && echo "OK: gerador reproduz o JSON atual"
```

Se o diff acusar diferenca, o gerador esta defasado — reconciliar antes de
seguir, senao o Step 3 vai reverter mudancas feitas na UI.

```bash
git add grafana/dashboards/build-performance-tracker-api.py
git commit -m "chore: version the dashboard generator alongside its output"
```

- [ ] **Step 1: Escrever a verificacao que deve falhar**

```bash
python3 -c "
import json
d=json.load(open('grafana/dashboards/performance-tracker-api.json'))
logs=[p for p in d['panels'] if p.get('type')=='logs']
print('paineis de log:', len(logs))
assert logs, 'nenhum painel de logs'
"
```

- [ ] **Step 2: Rodar e confirmar que falha**

Esperado: `AssertionError: nenhum painel de logs`.

- [ ] **Step 3: Adicionar a row e o painel ao gerador**

No script que gera o dashboard, apos a row `LearnUpon`:

```python
y += 8
panels.append(row("Logs", y))
y += 1
panels.append({
    "type": "logs", "title": "Errors and warnings", "id": nid(),
    "description": "stderr of both processes plus anything matching error/warn on stdout. "
                   "Level filtering is heuristic while the backend still logs through "
                   "console.* — see the Loki design spec.",
    "gridPos": {"h": 12, "w": 24, "x": 0, "y": y},
    "datasource": {"type": "loki", "uid": "loki"},
    # Duas queries, nao uma. LogQL nao aceita `or` entre seletores de stream:
    # `{stream="err"} or {stream="out"} |~ ...` e sintaxe invalida. O painel de
    # logs do Grafana aceita varios targets e faz a uniao na exibicao.
    #   A: todo o stderr, com ou sem a palavra "error" no texto
    #   B: so o stdout que casa error/warn — senao viria log informativo inteiro
    "targets": [
        {"datasource": {"type": "loki", "uid": "loki"},
         "expr": '{stream="err"}', "refId": "A", "queryType": "range"},
        {"datasource": {"type": "loki", "uid": "loki"},
         "expr": '{stream="out"} |~ "(?i)(error|warn)"', "refId": "B", "queryType": "range"},
    ],
    "options": {"showTime": True, "wrapLogMessage": True, "sortOrder": "Descending",
                "enableLogDetails": True, "dedupStrategy": "none", "prettifyLogMessage": False},
})
```

- [ ] **Step 4: Regerar e rodar a verificacao do Step 1**

Esperado: `paineis de log: 1`, sem AssertionError.

- [ ] **Step 5: Publicar e conferir que a query retorna dado**

```bash
scp grafana/dashboards/performance-tracker-api.json vps-obs:/opt/observability/grafana/dashboards/
sleep 15
ssh vps-obs 'cd /opt/observability && PW=$(grep "^GRAFANA_ADMIN_PASSWORD=" .env | cut -d= -f2-) && \
  curl -s -u "admin:$PW" -G --data-urlencode "query={stream=\"err\"}" \
  "https://teem.wagnerjunior.dev/api/datasources/proxy/uid/loki/loki/api/v1/query_range" | head -c 200'
```

Esperado: JSON com `"values"` preenchido.

- [ ] **Step 6: Confirmar que os painéis de metrica continuam intactos**

Rodar a validacao das 19 queries de Prometheus usada na Fase 5. Esperado: 19 com dado, 0 vazias, 0 erros.

- [ ] **Step 7: Commit**

```bash
git add grafana/dashboards/performance-tracker-api.json
git commit -m "feat: add an error and warning logs panel to the API dashboard"
```

---

## Encerramento

- [ ] Atualizar o README: secao de logs, geracao da credencial, e o runbook de migracao incluindo `pt-obs-loki-data`.
- [ ] Abrir o PR com o resumo dos criterios de aceite da spec, marcando o que foi verificado.
- [ ] Registrar na pagina do Notion que a Fase 7 saiu do "opcional" e esta implementada.

## Fora de escopo (confirmado na spec)

- Migrar `console.error`/`console.warn` para pino. Sem isso o filtro por nivel continua heuristico.
- Usuarios nominais no Grafana. O time aceitou seguir com admin unico, sem rastreabilidade de quem consulta PII.
- Regras de alerta sobre log — dependem do contact point do Teams (Fase 6, aguardando aprovacao).
