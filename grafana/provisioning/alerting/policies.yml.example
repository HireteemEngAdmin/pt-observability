# FUTURE PHASE (alerting): rename to policies.yml together with
# contactpoints.yml.example (instructions are there).
apiVersion: 1
policies:
  - orgId: 1
    receiver: teams-pt-alerts
    group_by: ['alertname']
